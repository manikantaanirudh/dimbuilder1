---
created: 2026-05-21T06:09:00.647Z
session: 58d2b58c-828e-4dd6-ab7c-b975a2db7832
working_directory: C:\Naga\projects\dimbuilder
---

## Context

Explored the `feature/production-readiness` branch which adds 9 features, none with dedicated tests. The existing test suite uses:
- `vitest` with `describe/it/expect`
- In-memory SQLite databases (`createDatabase(":memory:")`)
- Real Express servers on random ports (`listen(0)`)
- A `withServer(config, callback)` pattern for clean setup/teardown
- `defaultAppConfig` with overrides for test-specific config

## Implementation Steps

### 1. Create `src/test/basicAuth.test.ts` — Tests for Basic Auth middleware
- Test: no-op when `auth.enabled = false` (requests pass through)
- Test: returns 401 when auth enabled but no `Authorization` header
- Test: returns 401 for invalid credentials
- Test: returns 200 for valid credentials (base64-encoded `admin:changeme`)
- Test: health endpoint (`/api/health`) remains accessible without auth even when enabled

### 2. Create `src/test/rateLimiter.test.ts` — Tests for rate limiting
- Test: general rate limiter allows requests under limit
- Test: general rate limiter returns 429 after exceeding limit (use a low limit override in test)
- Test: heavy operation rate limiter returns 429 after exceeding import/export limit
- Note: The current code uses `isTest` detection to set limits to 10,000 in test mode. For testing the actual limiting behavior, we'll need to either: (a) test the module directly by importing the rate limit factories, or (b) mock `process.env.VITEST` to bypass. The simpler approach is to test by importing `express-rate-limit` directly with a low limit config in the test.

### 3. Create `src/test/validateMiddleware.test.ts` — Tests for Zod validation middleware
- Test: `validateBody` passes valid body through to handler
- Test: `validateBody` returns 400 with structured errors for invalid body
- Test: `validateBody` strips unknown fields (Zod strict mode behavior)
- Test with `createProjectSchema`: valid project creation body passes
- Test with `createProjectSchema`: missing name returns 400
- Test with `updateProjectSchema`: requires at least one field

### 4. Create `src/test/cors.test.ts` — Tests for CORS configuration
- Test: when no `corsOrigins` configured, all origins are allowed (default behavior)
- Test: when `corsOrigins` is set, only those origins get `Access-Control-Allow-Origin`
- Test: preflight OPTIONS request returns correct CORS headers

### 5. Create `src/test/logger.test.ts` — Tests for structured logging
- Test: `logger` exports a pino instance
- Test: `requestLogger` middleware calls `next()` and doesn't block requests
- Test: request logger records method, path, status, duration on response finish (verify by intercepting pino output)

### 6. Create `src/test/gracefulShutdown.test.ts` — Tests for graceful shutdown
- Test: server closes connections and DB on SIGTERM-like signal
- Since we can't easily send signals in tests, we'll test the shutdown logic by calling `server.close()` + `db.close()` pattern (which is what the signal handler does) and verify resources are cleaned up

### 7. Add auth config tests to `src/test/appConfig.test.ts` (edit existing file)
- Test: `defaultAppConfig` has `auth.enabled = false`
- Test: environment variable overrides (`AUTH_ENABLED`, `AUTH_USERNAME`, `AUTH_PASSWORD`) are applied by `loadAppConfig`
- Test: `ClientAppConfig` type excludes `auth` from client-safe config

## Verification

1. Run `npx vitest run` — all new + existing tests pass
2. Run `npx vitest --coverage` — coverage thresholds met (60% lines, 50% branches)
3. Verify zero regressions in the existing 210 tests

## Critical Files

- `src/server/middleware/basicAuth.ts` — Auth middleware to test
- `src/server/middleware/rateLimiter.ts` — Rate limiter to test
- `src/server/middleware/validate.ts` + `src/server/schemas.ts` — Zod middleware/schemas to test
- `src/test/api.test.ts` — Pattern to follow (beforeEach/afterEach server lifecycle)
- `src/test/exportGuards.test.ts` — Pattern to follow (`withServer` helper)