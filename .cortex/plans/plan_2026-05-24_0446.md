---
created: 2026-05-24T04:46:58.557Z
session: 3b414ff9-0063-42a2-ad27-caf68fd3a9ca
working_directory: C:\Naga\projects\dimbuilder
---

## Feature 4: ERP Source System Connectors

### Context
Features 1-3 complete (289 tests). The project has a connector pattern established in `src/server/connectors/onestream/` (interface + mock + factory). Feature 4 follows the same pattern for ERP systems.

### Implementation Steps
1. Create `src/shared/connectorTypes.ts` — types for connectors, mapping rules, sync jobs, source registry
2. Extend `src/server/db/schema.ts` — 5 tables (connector_definitions, mapping_rules, sync_jobs, sync_runs, member_source_registry)
3. Add repository methods for all connector tables
4. Create `src/server/connectors/mapping/mappingEngine.ts` — field transforms, filters, hierarchy builder, conflict detection
5. Create `src/server/connectors/erp/` — interface + mock connector + CSV connector skeleton + SQL skeleton
6. Create `src/server/routes/connectors.ts` — CRUD + sync operations + source registry
7. Wire into app.ts with auth guards
8. Add client API functions + ConnectorPanel component
9. Create `src/test/connectors.test.ts` — integration tests for CRUD, mapping transforms, sync execution
10. Run full suite, fix failures, commit

### Verification
- All 289+ existing tests pass
- New connector tests cover: CRUD, mapping transforms, filter rules, hierarchy building, sync run execution, credential redaction
- `npx vitest run --exclude 'src/test/workbookParser.test.ts'` passes

### Critical Files
- `src/server/connectors/mapping/mappingEngine.ts` — Core value: field transforms + hierarchy building
- `src/server/db/schema.ts` — 5 new tables
- `src/server/routes/connectors.ts` — REST API
- `src/server/connectors/erp/mockConnector.ts` — Testing foundation
- `src/test/connectors.test.ts` — Verification