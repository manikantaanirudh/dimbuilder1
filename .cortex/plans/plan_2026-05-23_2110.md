---
created: 2026-05-23T21:10:30.643Z
session: 0382b0bf-2ca5-4c27-a038-d1227c0ff1db
working_directory: C:\Naga\projects\dimbuilder
---

## Implementation Plan for Tasks 14-17

### Context
- `src/client/api/client.ts` has 7 API functions (`apiGet`, `apiPost`, `apiPatch`, `apiPut`, `apiPatchJson`, `apiDelete`, `apiText`) that use bare `fetch` with no auth headers
- `ClientAppConfig` omits auth — need `GET /api/auth/status` endpoint to expose `{ enabled, strategy }` to the client (this route is already under `/api/auth` which is unauthenticated)
- `src/client/auth/` directory doesn't exist yet
- Server auth is fully built: login, refresh, logout, register, tokens, middleware
- `repos.users` has `listUsers()`, `createUser()`, `findUserByEmail()`, and `hashPassword` is async

---

### Step 1: Add `GET /api/auth/status` endpoint (server)
**File:** `src/server/routes/auth.ts`
- Add a route `router.get("/status", ...)` returning `{ enabled: config.auth.enabled, strategy: config.auth.strategy, oidcAuthorizeUrl: config.auth.strategy === "oidc" ? "/api/auth/oidc/authorize" : null }`
- This is unauthenticated by design (under `/api/auth`)

### Step 2: Update `src/client/api/client.ts` (Task 14)
- Add module-level `accessToken`/`refreshToken` variables
- Add `setTokens`, `clearTokens`, `getAccessToken` exports
- Add `authHeaders()` helper
- Add `attemptRefresh()` helper
- Modify all 7 API functions to include `authHeaders()` in fetch + 401 refresh retry logic
- Add `apiLogin`, `apiLogout`, `apiGetMe`, `apiRegister` functions
- Add `fetchAuthStatus()` → calls `GET /api/auth/status`
- Import `LoginResponse`, `AuthUser` from `../../shared/authTypes`

### Step 3: Create `src/client/auth/AuthProvider.tsx` (Task 15)
- React context with `{ user, isAuthenticated, isLoading, authEnabled, login, logout }`
- On mount: call `fetchAuthStatus()` → if enabled, call `apiGetMe()` to restore session
- `login()` wraps `apiLogin`, sets user state
- `logout()` wraps `apiLogout`, clears user state
- Export `AuthContext`

### Step 4: Create `src/client/auth/useAuth.ts` (Task 15)
- Simple hook wrapping `useContext(AuthContext)`

### Step 5: Create `src/client/auth/LoginPage.tsx` (Task 15)
- Email + password form
- Error state display
- Calls `login()` from auth context on submit
- If OIDC strategy: show "Sign in with SSO" link

### Step 6: Create `src/client/auth/ProtectedRoute.tsx` (Task 15)
- If `!authEnabled`: render children
- If `isLoading`: show spinner
- If `!isAuthenticated`: show `<LoginPage />`
- If authenticated: render children

### Step 7: Create `src/client/auth/index.ts` (Task 15)
- Barrel file for clean imports

### Step 8: Modify `src/client/App.tsx` (Task 15)
- Import `AuthProvider`, `ProtectedRoute`
- Wrap `<AppShell>` in `<AuthProvider><ProtectedRoute>...</ProtectedRoute></AuthProvider>`

### Step 9: Modify `src/client/components/AppShell.tsx` (Task 16)
- Import `useAuth` hook
- In the header toolbar, after the brand/project-context area, add a user info section:
  - Display name or email
  - Role badge
  - Logout button
- Only render when `authEnabled` is true

### Step 10: Modify `src/server/index.ts` (Task 17)
- After `createDatabase()`, create repositories
- After DB init: check `config.auth.enabled && config.auth.strategy !== "none"`
- If true: check `repos.users.listUsers()` (filter out `local-admin` system user)
- If empty: hash a default password, call `repos.users.createUser(...)` with admin role
- Use env vars `ADMIN_EMAIL`/`ADMIN_PASSWORD` with defaults
- Log the result
- Pass `db` and `config` to `createApp` as before

### Step 11: Run tests
```
npx vitest run --exclude 'src/test/workbookParser.test.ts'
```

---

### Verification
- All existing tests pass (auth disabled by default = no-op middleware = no breaking changes)
- When auth is disabled: client works exactly as before (no tokens, headers empty, ProtectedRoute passes through)
- When auth is enabled: login page appears, tokens are stored, refresh logic works

### Critical Files
- `src/client/api/client.ts` — Core API client to modify with auth headers + refresh
- `src/server/routes/auth.ts` — Add status endpoint
- `src/client/App.tsx` — Wrap with auth context/protected route
- `src/client/components/AppShell.tsx` — Add user menu
- `src/server/index.ts` — Add admin seeding on startup