---
created: 2026-05-23T19:56:39.532Z
session: 3c94c2ab-612c-4a26-a20a-bdea23d91359
working_directory: C:\Naga\projects\dimbuilder
---

## Context

I explored the validation system and found the rules are defined in 3 places:
- `config/dimbuilder.yaml` — runtime YAML configuration
- `src/shared/appConfigDefaults.ts` — hardcoded TypeScript defaults
- `docs/validation-rules.md` — documentation

The current values are incorrect per OneStream official documentation:
- `memberNameMaxLength: 250` → should be **500**
- `restrictedCharacters: ["<", ">", "\"", "'", "&", "|", "[", "]", "\t", "\r", "\n"]` → missing 15+ characters, and `'` (single quote) shouldn't be there
- `reservedWords: ["Root", "None"]` → missing 30+ official reserved words

## Implementation Steps

### 1. Update `src/shared/appConfigDefaults.ts`
- Change `memberNameMaxLength` from `250` to `500`
- Replace `restrictedCharacters` array with the full official OneStream list: `/ | ! @ # , ; ^ * + - = \ ? < > " [ ] { } &` (remove `'` single quote; add all missing ones; keep `\t`, `\r`, `\n` as they are valid control-char catches)
- Expand `reservedWords` to include the full official list: `Account, All, Cons, Consolidation, Default, DimType, Entity, EntityDefault, Flow, IC, None, Origin, Parent, POV, Root, RootAccountDim, RootEntityDim, RootFlowDim, RootScenarioDim, RootUD1Dim, RootUD2Dim, RootUD3Dim, RootUD4Dim, RootUD5Dim, RootUD6Dim, RootUD7Dim, RootUD8Dim, Scenario, Time, UD1, UD2, UD3, UD4, UD5, UD6, UD7, UD8, UD1Default, Unknown, View, WF, Workflow, XFCommon`

### 2. Update `config/dimbuilder.yaml`
- Same changes as above to keep YAML and defaults in sync

### 3. Update `docs/validation-rules.md`
- Update the `memberNameMaxLength` value in the example YAML block
- Update the `restrictedCharacters` list
- Update the `reservedWords` list
- Add a note about `'` (single quote) NOT being restricted per official docs
- Add a note about `&` and `%` being soft restrictions (allowed but not recommended)
- Add mention of the 200-char description limit and alias uniqueness cross-validation

### 4. Update test file `src/test/oneStreamValidation.test.ts`
- The existing test uses `memberNameMaxLength: 8` for its own test case (overriding the default), so it should still pass
- However, needs to ensure the restricted character test still works (the test member `"Bad Name.Period<"` uses `<` which remains restricted)
- Add a new test case that verifies the single quote `'` is NOT flagged as restricted (regression test)
- Add a test that verifies a member at 499 chars passes but 501 chars fails (testing the new 500 limit)

### 5. Run tests and verify
- Run `npm test` to confirm all existing tests pass with the updated defaults
- Verify no regressions in the validation engine tests

## Verification
- Run `npm test` — all tests should pass
- Specifically check `oneStreamValidation.test.ts` and `validationEngine.test.ts`
- Verify the test for restricted characters still catches `<` (which remains in the list)
- Verify a new test confirms `'` does NOT trigger a restricted-character error

## Critical Files
- `src/shared/appConfigDefaults.ts` — Default values that drive validation behavior
- `config/dimbuilder.yaml` — Runtime YAML config that users see and can modify
- `src/shared/oneStreamValidation.ts` — Validation logic that consumes these configs
- `src/test/oneStreamValidation.test.ts` — Tests to update and verify
- `docs/validation-rules.md` — Documentation to keep accurate