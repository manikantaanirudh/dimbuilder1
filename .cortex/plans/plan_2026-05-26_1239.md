---
created: 2026-05-26T12:39:08.526Z
session: f55c1d91-acf1-46f2-8180-06d98415b17c
working_directory: C:\Naga\projects\dimbuilder
---

## Implementation Plan: Project Overview Audit Fixes

### Context
- **CSS**: Single `styles.css` file (3234 lines), all colors tokenized via CSS custom properties but no dark mode override block. 6+ hardcoded hex values in overview-related sections.
- **Components**: `Dashboard.tsx`, `KPICards.tsx`, `SnapshotManager.tsx`, `BlueprintStudio.tsx`, `ScoreRing.tsx`, `ui.tsx`
- **Existing patterns**: `ConfirmDialog` component already exists (with focus trap). `ToastProvider` exists with `aria-live`. Spacing tokens defined but unused in overview.
- **Entry**: `index.html` → `main.tsx` → `App.tsx` → `AppShell.tsx` → `Dashboard`

### Implementation Steps (6 phases)

#### Phase 1: Dark Mode & Theming (P1)
1. **Add `[data-theme="dark"]` CSS block** in `styles.css` after `:root` — override all `--bg`, `--surface`, `--text`, `--border`, `--muted`, `--primary`, `--accent`, `--danger`, `--warning`, `--success`, `--info` and their `-soft`/`-strong` variants with dark values.
2. **Replace hardcoded hex colors** throughout the file with new tokens:
   - `#b8d0ff` → `var(--info-border)`
   - `#b5dfbf` → `var(--success-border)`
   - `#f0c0a0` → `var(--warning-border)`
   - `#efb6c9` → `var(--danger-border)`
   - `#ffffff` in `.action-button.primary` → `var(--text-on-primary)`
   - `#37352f` in `.action-button.secondary` → `var(--text)`
   - `#b82626` → `var(--danger-strong)`
   - `#6b7280` → `var(--muted)`
   - `#9f2424` → `var(--danger-text)`
3. **Add theme toggle button** in `AppShell.tsx` header (toolbar area), using `Sun`/`Moon` icons from lucide-react. Store preference in `localStorage` and apply `data-theme` on `<html>`.
4. **Create `useTheme` hook** in `src/client/hooks/useTheme.ts` — reads localStorage, applies attribute to document, respects `prefers-color-scheme` as initial default.

#### Phase 2: Accessibility Fixes (P1/P2)
5. **Fix KPI cards**: Remove `cursor: pointer` from `.kpi-card` CSS and remove the hover transform, OR make them genuinely interactive buttons. Since they have no destination, remove the false affordance.
6. **Fix project title rename**: Add `tabIndex={0}`, `role="button"`, `aria-label="Rename project"`, and `onKeyDown` handler (Enter/Space triggers edit mode) to the `<h1>` in `Dashboard.tsx`.
7. **Add `aria-live="polite"` and `role="status"`** to:
   - `SnapshotManager.tsx` status `<p>` (line 118)
   - `BlueprintStudio.tsx` status `<p>` (line 175)
8. **Replace `window.confirm()`** in `SnapshotManager.tsx` with the existing `ConfirmDialog` component (already in codebase with focus trap, aria-modal, etc.).

#### Phase 3: Responsive Design (P2)
9. **Add `@media (max-width: 640px)` block** for:
   - `.blueprint-studio-form` → `grid-template-columns: 1fr`
   - `.snapshot-controls` → `grid-template-columns: 1fr`
   - `.overview-page` → collapse icon column (hide or shrink)

#### Phase 4: Performance (P2/P3)
10. **Memoize per-dimension issue summaries** in `Dashboard.tsx`: Add `useMemo` to pre-compute a `Map<string, IssueSummary>` before the `.map()` loop.
11. **Replace `transition: all`** on `button` in `styles.css:87` with specific properties: `background-color`, `border-color`, `color`, `box-shadow`.
12. **Add skeleton/shimmer state** to `KPICards.tsx` while data loads (replace `"—"` with a subtle pulse animation on the value area).

#### Phase 5: Polish (P3)
13. **Adopt spacing tokens** in overview CSS: Replace raw `18px` → `var(--space-md)`, `14px` → approximate custom value or keep (14 is between sm and md), `12px` → `var(--space-sm) + var(--space-xs)` or keep specific values where token gaps exist. Be pragmatic — only replace where a token is an exact match.
14. **Standardize font-size units** in KPI section: Change `.kpi-label` from `0.72rem` → `11px` (matches `.section-kicker`), `.kpi-value` from `1.5rem` → `24px` to match the px-based system.

#### Phase 6: Verification
15. Run `npm run build` (or `npx vite build`) to verify no TypeScript/compilation errors.
16. Visually verify dark mode toggle works, confirm the overview renders correctly in both themes.
17. Re-run `impeccable audit` to confirm score improvement.

### Verification
- `npx tsc --noEmit` — TypeScript checks pass
- `npx vite build` — Production build succeeds
- Manual visual check: dark mode toggle, overview panels, KPI cards, responsive at 640px
- Re-audit to confirm score improvement

### Critical Files
- `src/client/styles.css` — All CSS changes (dark mode, hardcoded colors, responsive, spacing tokens, button transition)
- `src/client/components/Dashboard.tsx` — Rename accessibility fix, memoization
- `src/client/components/KPICards.tsx` — Skeleton states, remove false affordance
- `src/client/components/SnapshotManager.tsx` — Replace window.confirm, add aria-live
- `src/client/components/AppShell.tsx` — Theme toggle button
- `src/client/hooks/useTheme.ts` — New file: theme hook (localStorage + prefers-color-scheme)