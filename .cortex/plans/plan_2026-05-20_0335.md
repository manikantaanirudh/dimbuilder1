---
created: 2026-05-20T03:35:47.403Z
session: 2db07758-98c0-4546-95d8-4564e2a2a624
working_directory: C:\Naga\projects\dimbuilder
---

## Context

**Key findings from exploration:**
- CSS variables are in `:root` block at lines 1-32 of `src/client/styles.css` — currently uses a purple `--primary: #5645d4`
- The toolbar is styled via `.toolbar` (line 349) and `.global-toolbar` (line 360). The toolbar background is `var(--surface)` (white)
- Brand mark at line 160 uses `var(--primary)` color and `var(--primary-soft)` background with a purple border
- `index.html` uses `/favicon.svg` and title "SR Onestream Dim Builder" — just need to update favicon path and title
- `public/favicon.svg` exists with a generic icon — we'll create `public/sr-logo.svg` instead
- `AppShell.tsx` uses a `<Database>` icon in the brand section at line 132 — we'll add the logo image
- Config router (`src/server/routes/config.ts`) has only a GET endpoint — need to add PUT
- `buildClientAppConfig` strips `paths` and `server` from the full config
- `src/client/api/client.ts` has `apiGet`, `apiPost`, `apiPatch`, `apiPatchJson`, `apiDelete` but no `apiPut`
- The `yaml` package is available (used in `loadAppConfig.ts`) and has a `stringify` export (yaml v2)
- Config is passed by reference to routers, so `Object.assign(config, validated)` will mutate it in-place
- The `loadAppConfig.ts` shows the config file path logic: `process.env.DIMBUILDER_CONFIG_FILE ?? "config/dimbuilder.yaml"`

## Implementation Steps

### Part A: Spaulding Ridge Branding

**Step 1: Update CSS variables** (`src/client/styles.css` lines 9-31)
- Replace the `:root` custom properties with the navy/gold/white palette (adding `--accent`, `--accent-strong`, `--accent-soft`, `--primary-soft` which don't exist yet)

**Step 2: Update toolbar/header styling** (`src/client/styles.css`)
- Modify `.toolbar` background at line 351 from `var(--surface)` to `var(--primary)`
- Add color: `#ffffff` to `.toolbar`
- Update `.brand-mark` (line 160) — remove the purple border, adjust for white icon on navy
- Update `.brand-wordmark` color from `var(--text)` to `#ffffff`
- Update `.project-context strong` and `.project-context span` to use white/light colors in the global toolbar
- Add hover styles for toolbar buttons using gold accent

**Step 3: Create `public/sr-logo.svg`**
- Simple SVG with navy background rect and gold "SR" text

**Step 4: Update `index.html`**
- Change favicon href from `/favicon.svg` to `/sr-logo.svg`
- Update title to "Spaulding Ridge Onestream Dim Builder"

**Step 5: Update `AppShell.tsx` header**
- Replace the `<Database>` icon brand-mark with an `<img src="/sr-logo.svg">` element
- Add `.app-logo` CSS class

### Part B: Frontend Config Editor

**Step 6: Add `apiPut` to `src/client/api/client.ts`**
- Add the generic `apiPut<T>` helper function following the existing `apiPost` pattern

**Step 7: Update config route** (`src/server/routes/config.ts`)
- Add imports: `writeFileSync` from `node:fs`, `stringify` from `yaml`, validation functions
- Add a PUT handler that:
  1. Validates the submitted body
  2. Merges with defaults, validates
  3. Writes to the YAML config file
  4. Mutates the in-memory config via `Object.assign(config, validated)`
  5. Returns the updated client config

**Step 8: Create `src/client/components/ConfigEditor.tsx`**
- JSON textarea editor component
- Loads current config as JSON, allows editing
- Saves via PUT `/api/config`
- Uses existing `ActionButton` from `./ui`

**Step 9: Integrate ConfigEditor into `AppShell.tsx`**
- Add `CONFIG_EDITOR_VALUE` constant
- Add a "Config" nav button in the sidebar (after the Admin button)
- Add rendering branch in the main content area

**Step 10: Add config editor CSS** (`src/client/styles.css`)
- `.config-editor-textarea` styles
- `.admin-status` style for status messages

### Verification

1. Run `npx vitest run` to ensure all existing tests pass
2. Verify the app starts with `npm run dev` and the branding appears correctly
3. Test the PUT `/api/config` endpoint returns updated config
4. Verify the ConfigEditor renders and can save changes

### Critical Files

- `src/client/styles.css` — All CSS variable and toolbar branding changes
- `src/server/routes/config.ts` — Add PUT endpoint for config writes
- `src/client/components/AppShell.tsx` — Logo integration and ConfigEditor nav
- `src/client/api/client.ts` — Add `apiPut` helper
- `src/client/components/ConfigEditor.tsx` — New component to create